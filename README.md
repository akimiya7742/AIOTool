# **WebTools \- Công Cụ Trực Tuyến Miễn Phí**

**WebTools** là một tập hợp các công cụ trực tuyến tiện ích, nhanh chóng và bảo mật, hoạt động hoàn toàn trên trình duyệt mà không cần cài đặt phần mềm.

## **🚀 Tính Năng Chính**

Dự án hiện đang cung cấp các công cụ sau:

* **Tạo & Scan mã QR**: Tạo mã QR cho URL, văn bản, Wifi và quét mã trực tiếp từ Camera hoặc file ảnh.  
* **Ghép nối PDF**: Kết hợp nhiều tệp PDF thành một tệp duy nhất một cách nhanh chóng.  
* **Ảnh sang PDF**: Chuyển đổi các định dạng hình ảnh (JPG, PNG) sang định dạng PDF chuyên nghiệp.  
* **Chế độ Giao diện**: Hỗ trợ đầy đủ Dark Mode (Giao diện tối) và Light Mode (Giao diện sáng).

## **🛠 Công Nghệ Sử Dụng**

* **HTML5 & CSS3**: Cấu trúc và phong cách giao diện.  
* **Tailwind CSS**: Framework CSS hiện đại giúp tùy chỉnh UI nhanh chóng và responsive.  
* **Font Awesome**: Hệ thống icon chuyên nghiệp.  
* **JavaScript (ES6+)**: Xử lý logic và các tính năng tương tác.

## **📂 Cấu Trúc Thư Mục**
```
AIOTools/  
├── index.html          # Trang chủ chính của dự án  
├── tools/              # Thư mục chứa các công cụ cụ thể  
│   ├── qrcode.html     # Công cụ QR Code  
│   ├── pdfmerge.html   # Công cụ Ghép PDF  
│   └── img2pdf.html    # Công cụ Ảnh sang PDF
│   └── ...

```
## **💻 Cài Đặt & Chạy Thử**

Bạn có thể chạy dự án này một cách đơn giản bằng cách clone kho lưu trữ và mở file `index.html` trong trình duyệt của mình:

1. Clone dự án:
```sh
git clone https://github.com/akimiya7742/AIOTool
```
2. Di chuyển vào thư mục:
```sh
cd AIOTool
```

3. Mở file `index.html` bằng trình duyệt web.

## **🤝 Đóng Góp**

Mọi đóng góp nhằm cải thiện các công cụ hiện có hoặc thêm các công cụ mới đều được hoan nghênh.

1. Fork dự án.  
2. Tạo nhánh tính năng mới (`git checkout -b feature/AmazingFeature`).  
3. Commit các thay đổi của bạn (`git commit -m 'Add some AmazingFeature'`).  
4. Push lên nhánh (`git push origin feature/AmazingFeature`).  
5. Mở một Pull Request.

## **📄 Bản Quyền**

Phân phối dưới giấy phép **MIT**. Xem LICENSE để biết thêm thông tin.

Được phát triển bởi [akimiya7742](https://github.com/akimiya7742) © 2026